algoritmo "conversor de grau"

var

    angulo , s : Real

inicio

    escreval("Digite um ângulo:")
    leia(angulo)
    s <- sen (grauprad(angulo))

    escreva("O seno de ", angulo , " é igual a " , s)

fimalgoritmo