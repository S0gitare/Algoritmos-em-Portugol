algoritmo "Salário"

var

    salario: Real
    X, A: real

inicio

    escreval("Digite seu salário: ")
    Leia(salario)

    X <- ((salario * 15) / 100)
    A <- (salario + X)

        escreval("O seu novo salário com aumento é de: " , A)

fimalgoritmo